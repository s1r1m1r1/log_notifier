name: log_notifier
version: 1.0.0
environment:
  sdk: ^3.9.0
  flutter: ^3.35.0
description: value notifier with log_notifier_observer.
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
resolution: workspace
